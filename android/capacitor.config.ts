import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "ar.portal659.print",
  appName: "Portal Print",
  webDir: "www",
  android: {
    allowMixedContent: true
  }
};

export default config;